doctype html

html
  head
    meta(charset="UTF-8 ")
    title 我的论坛 #{title}
    block scripts
      script(src='/jquery.min.js')
    style
      include css/layout.css

  body
    header
      a(href="/") 首页
      |
      |
      if loginName
        span(title=`hi, ${loginName}`) 欢迎,#{loginName}
        |
        |
        a(href="/add-post") 发帖
        |
        |
        a(href="/logout") 登出
      else
        a(href="/register") 注册
        |
        |
        a(href="/login") 登录

    block content
    block foot
      #footer
        p.copy &copy
